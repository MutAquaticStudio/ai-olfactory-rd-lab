# Optional graph-training environment.  Keep this out of the FastAPI app env.
# DeepChem currently supports Python >=3.7,<3.13; Python 3.11 is the primary
# supported training interpreter and Python 3.12 is used for compatibility CI.
-r requirements-training.txt
deepchem>=2.8.0,<3.0
